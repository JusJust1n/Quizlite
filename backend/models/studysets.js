const mongoose = require('mongoose')

const Schema = mongoose.Schema

const studysetsSchema = new Schema({
    title: {
        type: String,
        requried: true
    }
    cards: {
        type: 
    }
})